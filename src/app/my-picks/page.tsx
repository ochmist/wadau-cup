import { PageShell } from "@/components/PageShell";
import { MyPicksScreen } from "@/components/player/screens";

// Screen 7 — My Picks (wadau-webscreens → MyPicksBody).
export default function MyPicksPage() {
  return (
    <PageShell>
      <MyPicksScreen />
    </PageShell>
  );
}
