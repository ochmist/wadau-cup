import { ResetPasswordScreen } from "@/components/auth/ResetPasswordScreen";

// Screen 3 — Reset Password (forced on first login, before drafting).
export default function ResetPasswordPage() {
  return <ResetPasswordScreen />;
}
