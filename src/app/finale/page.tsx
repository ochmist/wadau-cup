import { PageShell } from "@/components/PageShell";
import { FinaleScreen } from "@/components/finale/FinaleScreen";

// Screen 12 — Season Finale & Payout (wadau-payout → PayoutBody).
export default function FinalePage() {
  return (
    <PageShell>
      <FinaleScreen />
    </PageShell>
  );
}
