import { PageShell } from "@/components/PageShell";
import { RulesScreen } from "@/components/rules/RulesScreen";

// Screen 10 — Rules & Scoring (wadau-rules → RulesBody).
export default function RulesPage() {
  return (
    <PageShell>
      <RulesScreen />
    </PageShell>
  );
}
