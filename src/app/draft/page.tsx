import { PageShell } from "@/components/PageShell";
import { DraftScreen } from "@/components/draft/DraftScreen";

// Screen 4 — Draft / Team Picking (wadau-webdraft → DraftBody).
export default function DraftPage() {
  return (
    <PageShell>
      <DraftScreen />
    </PageShell>
  );
}
