import { PageShell } from "@/components/PageShell";
import { ShareScreen } from "@/components/share/ShareScreen";

// Screen 15 — Share Card (wadau-screens-post → ScreenShare).
export default function SharePage() {
  return (
    <PageShell>
      <ShareScreen />
    </PageShell>
  );
}
